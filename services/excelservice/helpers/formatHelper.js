/**
 * The Formatter Function 
 * @param {*string[]} array The Array string 
 * @returns 
 */
const formatDatas = (str) => {
    let data = "{" + str + "}";
    return data;
}

module.exports = {formatDatas};